# Handoff — 2026-09-05

## What changed

Drafted 4 Lab Notes posts from real recent work, per the request. All committed locally on
`main` (commit `c91e5e1`), **not pushed** — nothing here is published or public.

New directory `drafts/` (sibling to `content/`, so Hugo's build never sees it — plus each file
carries `draft: true` as a second guard in case it's ever moved into `content/` unedited):

1. `drafts/a-tmux-estate-for-running-claude-everywhere-at-once.md` — the multi-session
   Claude Code tmux config in `claude-config` (commits `7c47586`, `3554062`): the
   attention/notification problem, the clipboard fallback, resurrect's relaunch-on-restore, and
   the same-day status-bar-position fix as the "what I'd do differently."
2. `drafts/why-my-site-screening-tool-refuses-to-give-you-a-score.md` — the NeoCloud site
   intelligence vertical slice (commit `4b6ac02`, `DECISIONS.md`): no composite score in the
   MVP, deterministic-API/QGIS-worker split, transactional package publishing, Hub-asset
   quarantine.
3. `drafts/the-ev-charger-that-lied-about-being-asleep.md` — the 2026-08-25 overnight EV
   charge failure in `home-energy` (commit `2483638`): two independent root causes (a 10-day
   integration outage that reverted schedule switches; a charger boost that's a timer, not a
   state), the discarded false-evidence trace, and the self-healing fixes deployed.
4. `drafts/shipping-a-mac-app-store-release-through-an-api-not-a-human.md` — the Distavo 1.9.1
   App Store resubmission (`distavo`, commit `d3d97fc` + the long tail of signing-CI fix
   commits): the microphone-prompt-wording rejection, and the automation's deliberate
   refuse-to-guess stop on `UNRESOLVED_ISSUES`.

Each post: what problem, what was built, what I'd do differently — first person, no employer
naming, house voice matched to the existing field-notes/leadership posts.

## Checklist compliance (employer/former-employer)

Read `content/disclaimer.md` and the `content/field-notes/_index.md` framing (the actual
"checklist" lives as *practice* embedded in those two files + prior commits `28b515a`/
`6f35a4c`, not as a separate standalone checklist doc — none exists in the repo; grepped and
confirmed). None of the four source projects touches an employer or former employer at all —
all four are Marc's own personal/side-project work — so no genericisation was needed on that
axis. Grepped all four drafts for `institute|employer|EMBL|password|token|api_key|secret`:
no matches.

Two discretionary calls made on privacy grounds (not employer-related, but same spirit as the
checklist's "don't overshare specifics the lesson doesn't need"):
- **NeoCloud post**: omits the actual candidate site name/location (private business
  screening target) — discusses the architecture/evidence-discipline decisions only.
- **EV post**: omits household/family specifics not needed for the technical lesson.

## Verified

- `git log --oneline -1` on `main` shows the new commit; `git status` is clean.
- `grep` across `drafts/` for employer names and secret-shaped strings: no matches (see above).
- Files are **not** under `content/`, so a `git push` today would not publish them — but I did
  **not** push, per "do not publish." Pushing is a one-line `git push` when Marc is ready to
  review/edit these before promoting any of them into `content/<section>/`.

## What's left / open questions for Marc

- **No target section decided yet.** These four don't fit either existing section as-is:
  `field-notes` is explicitly framed (in its `_index.md`) as 2016–2026 former-employer stories,
  and `leadership` is the CTO-programme/finance/ITIL knowledge base. These are personal
  build-log posts (self-hosted infra / product engineering), which is arguably closer to what
  the site's own tagline promises ("self-hosted AI infrastructure") than either existing
  section. Recommend a new section, e.g. `content/lab/` — happy to create the `_index.md` and
  move these in once you confirm a name.
- **No Vikunja project tracks lab-site/blog content work.** Searched `empresa/projecte` (id 7)
  and its children — no project or task matched `lab-site`, `blog`, or any of the four post
  topics. This was a one-off ad hoc request, not tied to existing tracked work, so I didn't
  create Vikunja artifacts for it (would have been busywork for a single content-drafting
  task). If posting to Lab Notes becomes a recurring cadence, worth a small Vikunja project
  under `empresa/projecte` to track the pipeline (draft → review → publish) the way other
  recurring work is tracked — flag if you want that set up.
- Not a blocker, but noting for completeness: I did not touch the `riera-blog-publish` skill —
  it already covers the section/date/gitleaks rules correctly for when these get promoted to
  `content/`; the only gap is it doesn't yet describe a draft-first step, which this session's
  `drafts/` pattern could fill in if you want it documented for reuse.

## Addendum — 2026-09-06 documentation review

**What changed**: Reviewed the `CLAUDE.md` (new) and `README.md` (Quick start + corrected
"Writing a post" section) drafted for this repo.

**Verified**: Spot-checked every concrete claim against the repo itself — `hugo.toml`,
`.github/workflows/hugo.yml` (Hugo `0.147.2`, gitleaks-then-build-then-deploy job order),
`content/field-notes/` and `content/leadership/` exist, `drafts/` holds the four files listed
above, `static/css/lab-site.css` has the `prefers-color-scheme: dark` block, `static/CNAME`
resolves to `blog.riera.co.uk`, and commits `28b515a`/`6f35a4c`/`fc3faf1` exist in `git log`.
No inaccuracies found; no edits were needed to either file.

**Left**: The open questions above (section name for the four drafts, whether to stand up a
Vikunja project) are still open — this pass was documentation-only.
