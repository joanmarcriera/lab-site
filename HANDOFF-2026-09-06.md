# Handoff — 2026-09-06

## What changed

**New `content/lab/` section**, resolving the open question from `HANDOFF-2026-09-05.md`
(the 4 drafts didn't fit `field-notes` — former-employer stories — or `leadership` — theory).

- `content/lab/_index.md` — new section for personal build-log posts (self-hosted infra,
  side-project engineering), explicitly distinguished from the other two sections in its blurb.
- Moved all 4 drafts from `drafts/` into `content/lab/`, flipped `draft: false`, and set each
  `date` to the underlying work's actual commit date (checked via `git log` in the source repo)
  so publish order is meaningful, since this blog shows no publish-date on the page:
  - `a-tmux-estate-for-running-claude-everywhere-at-once.md` → `2026-09-05`
  - `why-my-site-screening-tool-refuses-to-give-you-a-score.md` → `2026-09-03`
  - `the-ev-charger-that-lied-about-being-asleep.md` → `2026-08-26`
  - `shipping-a-mac-app-store-release-through-an-api-not-a-human.md` → `2026-08-11`
  - All are safely in the past relative to today (2026-09-06); none get silently dropped by Hugo.
- Committed locally (`87aa650`, `content(lab): promote 4 drafts into new lab section`).
  **Not pushed** — publishing stays your call, per the standing instruction.

**Added a "draft-first" step to the `riera-blog-publish` skill** (in `claude-config`, touched
only that one file): documents the `drafts/` + `draft: true` + `hugo server -D` preview pattern
used across both sessions, when to use it vs. going straight through the normal publish flow,
and the backdate-to-actual-work-date convention. Committed there too (`735c3c8`), not pushed.

**Created Vikunja project "Lab Notes blog"** (id 66, nested under empresa/projecte, id 7 —
`parent_project_id` verified after create per the safe-update rule) with one task per post,
each carrying a draft → review/promote → publish pipeline description and `percent_done: 66%`
(the first two stages are done; publish is pending your push):
- #1919 A tmux estate for running Claude everywhere at once
- #1920 Why my site-screening tool refuses to give you a score
- #1921 The EV charger that lied about being asleep
- #1922 Shipping a Mac App Store release through an API, not a human

## Verified

- Installed Hugo `0.165.0` via Homebrew (the CI-pinned `0.147.2` isn't in Homebrew's current
  formula; close enough for a local smoke test — CI still builds with the pinned version).
- `hugo --minify`: clean build, 109 pages, all 4 new posts present under `public/lab/`.
- `gitleaks detect --source .` (full commit history) and `--source . --no-git` (working tree):
  both **no leaks found**.
- `git status` clean after commit; `content/lab/` staged and committed as one logical unit.
- Vikunja project 66: `GET` after create confirms `parent_project_id: 7`. All 4 tasks confirmed
  created under project 66 with `percent_done: 0.66`.

## What's left

- **Push is yours.** Local `main` is 4 commits ahead of `origin/main` (the 3 pre-existing ones
  from 2026-09-05 plus this session's `87aa650`). `git push origin main` when you're ready to
  make `content/lab/` live — GitHub Actions will run gitleaks → Hugo build → Pages deploy.
- **claude-config commit `735c3c8` also not pushed** — same reasoning (didn't want to push
  anything without being asked); push it whenever convenient, it's config-repo only, no
  auto-deploy risk.
- Mark the 4 Vikunja tasks (#1919–#1922) `done` once you've pushed and verified each post live.

## Notes

- Ran into a **guardrails-hook false positive** while working the unrelated datacentre-knowledge
  push in parallel this session: the hook resolves the repo from the session's tracked cwd,
  which stays pinned to `lab-site` regardless of an in-command `cd`, so any `git push` in this
  session gets misattributed to `lab-site`'s auto-deploying `main`. Not a lab-site issue, just
  noting it in case it bites again — worth a look if you want the hook to trust the command's
  actual `cd` target instead.
